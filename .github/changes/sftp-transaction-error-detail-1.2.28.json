{
  "version": 1,
  "commit_message": "Expose SFTP Samba transaction error details",
  "replacements": [
    {
      "path": "sftp/sambaPoint.py",
      "old": "__INIT_LOCK__=threading.Lock()\n\"\"\"Zámek pro inicializaci.\"\"\"\n\nclass smbHelp:",
      "new": "__INIT_LOCK__=threading.Lock()\n\"\"\"Zámek pro inicializaci.\"\"\"\n\nclass ManagedCIFSTargetNotEmptyError(RuntimeError):\n    \"\"\"Managed CIFS target contains unexpected local data while unmounted.\"\"\"\n    def __init__(self, target:str):\n        self.target = target\n        super().__init__(\n            f\"Managed CIFS target {target} is not empty while unmounted; \"\n            \"refusing to hide underlying data beneath the mount.\"\n        )\n\nclass smbHelp:",
      "expected": 1
    },
    {
      "path": "sftp/sambaPoint.py",
      "old": "    _batchDepth:int = 0\n    \"\"\"Hloubka vnořené dávky změn; post-processing proběhne až při návratu na nulu.\"\"\"",
      "new": "    _batchDepth:int = 0\n    \"\"\"Hloubka vnořené dávky změn; post-processing proběhne až při návratu na nulu.\"\"\"\n    lastError:Exception|None = None\n    \"\"\"Poslední konkrétní chyba nejvyšší Samba/CIFS batch transakce.\"\"\"",
      "expected": 1
    },
    {
      "path": "sftp/sambaPoint.py",
      "old": "    def beginBatch()->None:\n        \"\"\"Zahájí (případně vnoří) dávku Samba/CIFS změn.\"\"\"\n        smbHelp._batchDepth += 1",
      "new": "    def beginBatch()->None:\n        \"\"\"Zahájí (případně vnoří) dávku Samba/CIFS změn.\"\"\"\n        if smbHelp._batchDepth == 0:\n            smbHelp.lastError = None\n        smbHelp._batchDepth += 1",
      "expected": 1
    },
    {
      "path": "sftp/sambaPoint.py",
      "old": "            if os.listdir(target):\n                raise RuntimeError(\n                    f\"Managed CIFS target {target} is not empty while unmounted; \"\n                    \"refusing to hide underlying data beneath the mount.\"\n                )",
      "new": "            if os.listdir(target):\n                raise ManagedCIFSTargetNotEmptyError(target)",
      "expected": 1
    },
    {
      "path": "sftp/sambaPoint.py",
      "old": "        pending = smbHelp.requireSambaRestart or bool(smbHelp.toMount) or bool(smbHelp.toRemove)\n        if not pending:",
      "new": "        smbHelp.lastError = None\n        pending = smbHelp.requireSambaRestart or bool(smbHelp.toMount) or bool(smbHelp.toRemove)\n        if not pending:",
      "expected": 1
    },
    {
      "path": "sftp/sambaPoint.py",
      "old": "        except Exception as e:\n            log.error(f\"Samba/CIFS mountpoint transaction failed: {e}\")\n            log.exception(e)\n            return False",
      "new": "        except Exception as e:\n            smbHelp.lastError = e\n            log.error(f\"Samba/CIFS mountpoint transaction failed: {e}\")\n            log.exception(e)\n            return False",
      "expected": 1
    },
    {
      "path": "tests/test_sftp_rw_reconcile.py",
      "old": "        samba_module.smbHelp.toCloseShares.clear()\n\n    def tearDown(self):",
      "new": "        samba_module.smbHelp.toCloseShares.clear()\n        samba_module.smbHelp.lastError = None\n\n    def tearDown(self):",
      "expected": 1
    },
    {
      "path": "tests/test_sftp_rw_reconcile.py",
      "old": "        samba_module.smbHelp.toCloseShares.clear()\n\n    def test_post_remove_does_not_cleanup_inside_active_batch(self):",
      "new": "        samba_module.smbHelp.toCloseShares.clear()\n        samba_module.smbHelp.lastError = None\n\n    def test_post_remove_does_not_cleanup_inside_active_batch(self):",
      "expected": 1
    },
    {
      "path": "tests/test_sftp_rw_reconcile.py",
      "old": "    def test_nonempty_unmounted_target_is_never_hidden_by_remount(self):\n        with tempfile.TemporaryDirectory() as tmp:\n            target = Path(tmp) / \"docs\"\n            target.mkdir()\n            (target / \"stale.txt\").write_text(\"stale\", encoding=\"utf-8\")\n            with self.assertRaises(RuntimeError):\n                samba_module.smbHelp.prepareConfiguredMountpointDirectories({str(target)})",
      "new": "    def test_nonempty_unmounted_target_is_never_hidden_by_remount(self):\n        with tempfile.TemporaryDirectory() as tmp:\n            target = Path(tmp) / \"docs\"\n            target.mkdir()\n            (target / \"stale.txt\").write_text(\"stale\", encoding=\"utf-8\")\n            with self.assertRaises(samba_module.ManagedCIFSTargetNotEmptyError) as ctx:\n                samba_module.smbHelp.prepareConfiguredMountpointDirectories({str(target)})\n            self.assertEqual(ctx.exception.target, str(target))\n\n    def test_finalize_exposes_concrete_transaction_error(self):\n        with tempfile.TemporaryDirectory() as tmp:\n            target = Path(tmp) / \"docs\"\n            target.mkdir()\n            (target / \"stale.txt\").write_text(\"stale\", encoding=\"utf-8\")\n            configured = [(\"//127.0.0.1/sftp_mount_alice_docs\", str(target))]\n            samba_module.smbHelp.requireSambaRestart = True\n            with patch.object(samba_module.smbHelp, \"getConfiguredManagedCIFS\", return_value=configured), patch.object(samba_module.smbHelp, \"unmountAllManagedCIFS\"), patch.object(samba_module.smbHelp, \"removeQueuedMountpointDirectories\", return_value=True):\n                self.assertFalse(samba_module.smbHelp.finalizeMountpointChanges())\n            self.assertIsInstance(samba_module.smbHelp.lastError, samba_module.ManagedCIFSTargetNotEmptyError)\n            self.assertEqual(samba_module.smbHelp.lastError.target, str(target))",
      "expected": 1
    },
    {
      "path": "__init__.py",
      "old": "__version__ = \"1.2.27\"\n\n# 1.2.27:",
      "new": "__version__ = \"1.2.28\"\n\n# 1.2.28: Samba/CIFS batch failures expose their concrete exception; non-empty managed targets use a typed error with target path\n# 1.2.27:",
      "expected": 1
    }
  ]
}
