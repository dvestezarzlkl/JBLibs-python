{
  "version": 1,
  "commit_message": "Preserve only non-empty SFTP mount targets for backup",
  "replacements": [
    {
      "path": "sftp/mounts.py",
      "old": "import os,time",
      "new": "import errno,os,time",
      "expected": 1
    },
    {
      "path": "sftp/mounts.py",
      "old": "    def deleteMountpoint(self, mount_name:str|None, preserveTargetDirs:bool=False)->None:\n        \"\"\"Odstraní mountpoint s daným jménem z uživatelova jailu.",
      "new": "    @staticmethod\n    def _cleanupPreservedTargetDirs(targets:list[str])->None:\n        \"\"\"After physical unmount, remove empty preserved target directories.\n\n        Non-empty targets are intentionally kept so unexpected underlying data\n        is included in the pre-delete user backup. Other filesystem errors are\n        treated as unsafe and abort the teardown.\n        \"\"\"\n        for target in targets:\n            try:\n                os.rmdir(target)\n                log.debug(f\"Removed empty preserved SFTP mount target {target}.\")\n            except FileNotFoundError:\n                continue\n            except OSError as e:\n                if e.errno == errno.ENOTEMPTY:\n                    log.warning(\n                        f\"Preserving non-empty SFTP mount target {target} for pre-delete backup.\"\n                    )\n                    continue\n                raise RuntimeError(\n                    f\"Failed to clean preserved SFTP mount target {target}: {e}\"\n                ) from e\n\n    def deleteMountpoint(self, mount_name:str|None, preserveTargetDirs:bool=False)->None:\n        \"\"\"Odstraní mountpoint s daným jménem z uživatelova jailu.",
      "expected": 1
    },
    {
      "path": "sftp/mounts.py",
      "old": "        if mount_name is None:\n            for mp in self.mountpoints[:]:  # kopie seznamu pro bezpečné mazání během iterace\n                self.deleteOneMountpoint(mp, preserveTargetDir=preserveTargetDirs)\n        else:\n            mp=self.getMountpointByName(mount_name)\n            if mp is None:\n                raise RuntimeError(f\"Mountpoint {mount_name} does not exist for user {self.username}.\")\n            self.deleteOneMountpoint(mp, preserveTargetDir=preserveTargetDirs)\n\n        if not smb.postRemoveAllMountpoints():\n            raise RuntimeError(\"Failed to finalize mountpoint removal changes.\")",
      "new": "        preserved_targets:list[str] = []\n        if preserveTargetDirs:\n            if mount_name is None:\n                preserved_targets = [mp.mountPath for mp in self.mountpoints]\n            else:\n                mp = self.getMountpointByName(mount_name)\n                if mp is not None:\n                    preserved_targets = [mp.mountPath]\n\n        if mount_name is None:\n            for mp in self.mountpoints[:]:  # kopie seznamu pro bezpečné mazání během iterace\n                self.deleteOneMountpoint(mp, preserveTargetDir=preserveTargetDirs)\n        else:\n            mp=self.getMountpointByName(mount_name)\n            if mp is None:\n                raise RuntimeError(f\"Mountpoint {mount_name} does not exist for user {self.username}.\")\n            self.deleteOneMountpoint(mp, preserveTargetDir=preserveTargetDirs)\n\n        if not smb.postRemoveAllMountpoints():\n            raise RuntimeError(\"Failed to finalize mountpoint removal changes.\")\n\n        if preserveTargetDirs and preserved_targets:\n            self._cleanupPreservedTargetDirs(preserved_targets)",
      "expected": 1
    },
    {
      "path": "tests/test_sftp_rw_reconcile.py",
      "old": "samba_module = importlib.import_module(\"libs.JBLibs.sftp.sambaPoint\")\nparser_module = importlib.import_module(\"libs.JBLibs.sftp.parser\")",
      "new": "samba_module = importlib.import_module(\"libs.JBLibs.sftp.sambaPoint\")\nmounts_module = importlib.import_module(\"libs.JBLibs.sftp.mounts\")\nparser_module = importlib.import_module(\"libs.JBLibs.sftp.parser\")",
      "expected": 1
    },
    {
      "path": "tests/test_sftp_rw_reconcile.py",
      "old": "class SftpUserCleanupTests(unittest.TestCase):\n    def _fake_user(self, home: str):",
      "new": "class SftpUserCleanupTests(unittest.TestCase):\n    def test_backup_target_cleanup_removes_empty_and_preserves_nonempty(self):\n        with tempfile.TemporaryDirectory() as tmp:\n            empty_target = Path(tmp) / \"empty\"\n            dirty_target = Path(tmp) / \"dirty\"\n            empty_target.mkdir()\n            dirty_target.mkdir()\n            stale = dirty_target / \"stale.txt\"\n            stale.write_text(\"stale\", encoding=\"utf-8\")\n\n            mounts_module.mountpointsManager._cleanupPreservedTargetDirs([\n                str(empty_target),\n                str(dirty_target),\n            ])\n\n            self.assertFalse(empty_target.exists())\n            self.assertTrue(dirty_target.is_dir())\n            self.assertTrue(stale.is_file())\n\n    def _fake_user(self, home: str):",
      "expected": 1
    },
    {
      "path": "__init__.py",
      "old": "__version__ = \"1.2.26\"\n\n# 1.2.26:",
      "new": "__version__ = \"1.2.27\"\n\n# 1.2.27: pre-delete SFTP backup drops empty detached mount targets and preserves only targets containing underlying data\n# 1.2.26:",
      "expected": 1
    },
    {
      "path": ".github/workflows/test-sftp-rw-reconcile.yml",
      "old": "assert v == \"1.2.26\"",
      "new": "assert v == \"1.2.27\"",
      "expected": 1
    },
    {
      "path": ".github/workflows/test-fs-utils.yml",
      "old": "assert version == \"1.2.26\"",
      "new": "assert version == \"1.2.27\"",
      "expected": 1
    }
  ]
}
